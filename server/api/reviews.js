const router = require("express").Router();
// const { prisma } = require("../db");

// GET route '/reviews' -  to fetch all reviews

// GET '/reviews/me' - to fetch fetch all reviews written by a registered user

// POST '/reviews' - to submit a review

// PUT '/reviews/:id' - to update a review

// DELETE '/reviews/:id/' - to delete a review

module.exports = router;
